# Not for the public repo — kept local only.
# Brewfile is this Mac's full installed-software inventory (GUI apps, ngrok, tailscale,
# ML/research + project libs). bootstrap.sh reads it locally; it's deliberately not published.
Brewfile

# macOS noise
.DS_Store
