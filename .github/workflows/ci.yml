name: ci
on: [push, pull_request]
permissions:
  contents: read

jobs:
  check:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0
      - uses: jdx/mise-action@v3
        with:
          install: false

      - name: install locked tools
        run: |
          mise trust
          mise install
        env:
          MISE_LOCKED: "1"

      - name: secrets (full history, repo rules)
        run: mise run secrets

      - name: shellcheck
        run: mise run lint

      - name: render templates + zsh -n
        run: mise run render

      - name: check non-templated configs
        run: mise run check-static
