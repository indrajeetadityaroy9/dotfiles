#!/bin/sh
# Brewfile sha256: {{ include "Brewfile" | sha256sum }}
set -eu

eval "$(/opt/homebrew/bin/brew shellenv)"
brew bundle install --no-upgrade --file={{ joinPath .chezmoi.sourceDir "Brewfile" | quote }}
